# Google-Style Random Password Generator 
# Learning Objectives: 
        # Use python string module to access letters, digits and symbols 
        # Using 'random' function to pick random characters
        # Combining everything into a pool of characters
        # Asking the user for custom p.word length, and preferences (letters : int)

import string 
import random 
# make a pool of all characters (letters, digits, symbol)
all_chars = string.ascii_letters + string.digits + string.punctuation

int(input("How many letters do you want?"))
int
